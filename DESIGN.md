# 오늘 점심 취향 예측 서비스 디자인 계약

## 1. 제품 분위기와 디자인 의도

- 교실에서 설명을 길게 읽지 않아도 순서대로 진행할 수 있는 실험 기록지에 가깝게 만든다.
- 한 화면에서 `30개 음식 평가 → 오늘 점심 예측 → 식후 비교`의 세 단계만 보인다.
- 기존 Gradio의 안정적인 기본 컴포넌트와 반응형 동작을 유지한다.
- 시각적 변화량은 3/10, 동작 변화량은 2/10, 정보 밀도는 5/10을 기준으로 한다.

## 2. 색상 체계

- 배경과 카드, 본문, 테두리는 현재 Gradio 테마의 중립 색상을 그대로 사용한다.
- 주요 행동은 테마의 primary 색상 하나로 통일한다.
- 성공·주의·오류는 색만으로 구분하지 않고 제목과 문장으로 상태를 함께 표시한다.
- 새 화면에 임의의 브랜드 색상이나 장식용 그라데이션을 추가하지 않는다.

## 3. 글꼴과 글자 위계

- 한글 표시가 안정적인 Gradio 기본 시스템 산세리프 글꼴을 사용한다.
- 페이지 제목은 H1, 각 단계는 H2, 단계 안의 보조 설명은 일반 본문을 사용한다.
- 버튼과 입력 라벨은 짧은 행동 문장으로 작성하고, 수식이나 전문 용어는 결과 설명 안에서만 쓴다.

## 4. 간격과 반응형 배치

- Gradio 기본 4px 간격 단위를 유지한다.
- 큰 화면에서는 이름 입력과 버튼, 예측 결과와 메뉴 표처럼 함께 읽는 요소만 한 행에 둔다.
- 768px 이하에서는 행을 세로로 쌓아 가로 스크롤과 잘린 버튼을 방지한다.
- 30개 평가표는 표 자체의 최소 폭이 필요한 컴포넌트이므로 작은 화면에서 표 내부 가로 스크롤을 허용한다.

## 5. 컴포넌트와 상태

- 단계 1: 이름 입력, 평가표 불러오기, 30개 음식 평가표, 평점 저장.
- 단계 2: 오늘 메뉴 안내, 예측 실행, 메뉴별 예상 평점과 근거 표.
- 단계 3: 실제 만족도(1~5), 예측과 비교, 오차 결과.
- 초기 상태에는 다음 행동을 안내하고, 성공 상태에는 저장 개수나 예측 점수를 숫자로 보여 준다.
- 평가가 30개 미만인 상태, 저장 전 예측, 예측 전 실제 비교는 짧고 구체적인 오류 문장으로 막는다.

## 6. 움직임

- Gradio가 제공하는 로딩 표시와 포커스 전환 외에 장식용 애니메이션을 사용하지 않는다.
- 결과는 버튼 실행 뒤 같은 단계 아래에 나타나며 자동 스크롤이나 깜빡임을 만들지 않는다.
- 운영체제의 reduced-motion 설정을 방해하는 별도 CSS를 두지 않는다.

## 7. 깊이와 표면

- 기본 Blocks, Row, Group의 테두리와 배경만 사용한다.
- 과한 그림자, 떠 있는 카드, 중첩된 카드 구조를 만들지 않는다.
- 각 단계는 제목과 얇은 구분선으로 구별하고 결과는 입력 바로 아래에 둔다.

## 8. 접근성·검증·남은 제약

- 모든 입력에는 보이는 한국어 라벨을 둔다. 버튼은 동사로 시작하고 결과에는 단위를 표기한다.
- 로컬과 LAN 모드 모두 키보드만으로 이름 입력, 표 평점 수정, 저장, 예측, 비교가 가능해야 한다.
- 375px, 768px, 1280px에서 초기·불완전 평가·저장 완료·예측 완료·실제 비교 상태를 확인한다.
- 컴포넌트 쇼케이스에 해당하는 검증 화면은 별도로 만들지 않고, 실제 한 화면의 세 단계와 각 상태를 그대로 사용한다.
- Gradio Dataframe은 작은 화면에서 내부 스크롤이 필요한 제약을 허용하되 페이지 전체가 가로로 넘치지 않아야 한다.
