# 실행·검증 보고서

## 검증 환경

- 검증일: 2026-07-26
- 운영체제: Windows
- Python: 3.12.13 Codex 번들 런타임
- 기본 학교: 남악고등학교
- 공식 NEIS 코드: `Q10 / 7140272`

## 자동 테스트

실행 명령:

```powershell
python -m pytest -q
```

결과:

```text
......................................                                   [100%]
38 passed in 0.35s
```

검증 범위:

- 날짜 범위와 잘못된 입력 차단
- 양 끝을 포함한 366일 범위와 367일 차단
- 학교 검색·급식 API 응답 변환·다중 페이지 수집
- 빈 결과·서버 오류 처리
- 메뉴 HTML·알레르기·영양 정보 전처리
- TF-IDF 취향 우선순위
- 피하고 싶은 키워드 감점
- 알레르기 번호 일치 메뉴 제외
- 추천 점수 0~100 제한
- K-Means 상대 군집 이름과 영양 정보 부족 처리
- 실시간 실패 시 요청 날짜와 겹치는 남악고 예비 데이터만 전환
- 요청 날짜와 예비 날짜가 겹치지 않을 때의 명시적 기간 안내
- 공개 20점 기준점과 가감점 회귀 시험
- Colab 공개 링크·로컬 비공개 실행 옵션
- Colab JSON 생성과 전체 추천 흐름 실행

## 학생용 Colab 전체 실행

실행 명령:

```powershell
python scripts/build_colab.py
python scripts/verify_colab.py
```

결과:

- 노트북 전체 셀: 29개
- 순서대로 실행된 Python 셀: 16개
- 데이터 출처: 내장 NEIS 예비 데이터
- 정제된 급식 행: 5개
- 생성된 추천: 3개
- 1위 점수: 52.9점
- 모델 카드: 생성 완료
- Gradio 화면 함수: 정의 완료
- 자동 검증에서는 서버를 열지 않고 UI 콜백·Gradio Blocks 생성·실행 옵션을 별도 확인

## Gradio 서비스 콜백

익명 빈 취향 프로필과 예비 데이터 기간으로 `app.recommend_for_ui`를 직접 실행했다.

```text
app_callback_ok rows=3 top_score=17.0
```

콜백이 데이터 로드, 추천, 한국어 안전 문구, 결과 표 3행을 반환했다. Gradio 6.20.0 환경에서 `build_demo()`가 `Blocks` 객체를 생성하는 것도 확인했다.

## Colab 화면 접속 방식

`launch_options`의 환경별 결과:

```text
Colab: {'share': True, 'debug': False, 'show_error': True}
로컬:  {'share': False, 'debug': False, 'show_error': True}
```

Colab에서는 localhost가 아닌 임시 공개 링크를 만들고, 로컬에서는 외부 공유를 끈다. 학생 노트북과 화면 모두 실제 알레르기·질병 정보를 입력하지 말라는 경고를 표시한다. 이 로컬 검증에서는 실제 Colab 런타임의 외부 링크를 열지 않았으며, 대신 독립 실행형 노트북 전체 코드 흐름, 환경 판별 함수, Gradio Blocks 생성을 각각 검증했다.

## 실시간 NEIS 확인

### 급식 엔드포인트

확인된 공식 코드로 2026-06-24 급식을 조회했다.

```text
live_meal_ok code=Q10/7140272 rows=2 date=20260624
```

### 학교 검색 엔드포인트

설계·예비 데이터 생성 시에는 남악고등학교를 `Q10 / 7140272`로 정상 조회했다. 최종 스모크 테스트 시점에는 `schoolInfo`가 다음 일시적 서버 오류를 반환했다.

```text
NEIS 오류: 서버 오류입니다. 지속적으로 발생시 홈페이지로 문의(Q&A) 바랍니다.
```

이 상황에서 수업이 중단되지 않도록 서비스가 요청 날짜와 겹치는 남악고 예비 데이터로 전환되는 것을 별도 테스트로 확인했다. 날짜가 겹치지 않으면 예비 자료의 실제 기간을 안내하고 멈추며, 다른 날짜의 행을 몰래 대신 보여 주지 않는다.

## 예비 데이터

- 출처: NEIS 교육정보 개방 포털
- 학교: 남악고등학교
- 조회 범위: 2026-01-01~2026-12-31
- 현재 API가 반환한 중식: 5건
- 실제 포함 날짜: 2026-06-24~2026-06-30
- 개인 식별정보: 없음

5건은 군집화와 추천 흐름을 실행하기에는 충분하지만 일반화된 만족도나 영양 결론을 내리기에는 부족하다. 학생 데이터 카드와 모델 카드에 이 제한을 명시했다. API에 데이터가 추가되면 `scripts/refresh_sample_data.py`로 같은 형식의 예비 자료를 다시 만들 수 있다.

## 수업 문서 점검

- 1회차: 8개 활동 블록, 180분
- 2회차: 8개 활동 블록, 180분
- 3회차: 8개 활동 블록, 180분
- 4회차: 8개 활동 블록, 180분
- 5회차: 9개 활동 블록, 180분
- 6회차: 9개 활동 블록, 180분
- 5회차 종료 기준, 6인 순환 역할표, 100점 루브릭 확인
- 개인정보 금지 항목과 영양사 재확인 문구 확인
- `TBD`, `TODO`, 미완성 구현 지시 없음

## 환경 조정 사항

Windows 애플리케이션 제어 정책이 작업 폴더에 새로 설치한 Pandas·scikit-learn DLL을 차단했다. 검증에는 Codex 번들에 포함된 허용된 Pandas·NumPy를 사용했다. 추천 원리는 scikit-learn 호출 대신 작은 문자 n-gram TF-IDF와 K-Means를 Python·NumPy로 직접 구현했다.

이 조정의 교육적 장점은 학생이 벡터화와 군집 반복 과정을 코드에서 직접 추적할 수 있다는 점이다. 학생용 Colab은 추가 머신러닝 패키지 없이 실행되며 Gradio가 없는 경우에만 설치한다.

## 남은 운영상 제한

- NEIS 데이터와 실제 학교 급식은 변경될 수 있다.
- 메뉴 문자열에 적히지 않은 숨은 재료를 알 수 없다.
- 알레르기 번호 제외는 보조 경고이며 안전을 보장하지 않는다.
- Colab Gradio 주소는 임시 공개 링크이므로 실제 의료 정보를 입력하지 않는다.
- 추천 점수는 취향 유사도이지 건강·의학·실제 만족도 점수가 아니다.
- `삼향고등학교`는 공식 학교명으로 확인되지 않아 기본 목록에 넣지 않았다.
